# Keep the build context lean: the WAR is rebuilt from source inside the
# image, so host build outputs and dependency trees are dead weight.
.git
.gitignore
target
**/target
**/node_modules
src/main/frontend/node
src/main/frontend/target-frontend
src/main/frontend/dist
*.log
